**IconButton** — a square beveled block holding one icon; use in toolbars, cards, nav. Always pass `label` for accessibility.

```jsx
<IconButton label="Menu" variant="accent"><iconify-icon icon="pixelarticons:menu"></iconify-icon></IconButton>
```

Sizes `sm | md | lg`. Variants `default | accent | ghost`.
