**ProgressBar** — segmented "stacked block" meter. Great for skill levels, loading, stats.

```jsx
<ProgressBar value={70} piece="s" label="C++" />
```
`cells` sets segment count; `piece` sets fill color.
