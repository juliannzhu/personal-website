**Tag** — keyword / skill chip with a colored block marker. Use for tech stacks, post categories, filters.

```jsx
<Tag piece="i">React</Tag>
<Tag piece="t" interactive active>Algorithms</Tag>
```
`interactive` adds hover; `active` shows selected state.
