**Input** — single-line text field with label, hint, and error states.

```jsx
<Input label="Email" placeholder="hi@juliann.dev" hint="Never shared." />
<Input label="Handle" error="Already taken." />
```
Passing `error` turns the field red and replaces the hint.
